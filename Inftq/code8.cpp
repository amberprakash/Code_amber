#include<iostream>
#include<vector>
#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,m,k;
    cin>>n>>m>>k;
    vector<int>ball(n);
    for(int i=0;i<n;i++){
        cin>>ball[i];
    }
    vector<vector<int>>cost(n,vector<int>(m));

    return 0;
}